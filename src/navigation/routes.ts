const ROUTES = {
    HOME: '/',
    LOGIN: '/login',
    GENERAL_ERROR: '/general-error',
    ALL: '*',
    SESSION_EXPIRED: '/session-expired',
    FORBIDDEN: '/forbidden',
    USER: {
        CREATE: '/user/create',
    },
};

export default ROUTES;
